# Generated by Cargo
# will have compiled files and executables
/target/

# This crate is consumed as a path dependency, so the app's own
# src-tauri/Cargo.lock is what resolves it. This one only appears when cargo is
# run with this directory as its workspace root (e.g. running these unit tests),
# and it affects nothing.
/Cargo.lock

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas
