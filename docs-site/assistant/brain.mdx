---
title: "Brain: local or cloud"
description: "Run the SpeakoFlow assistant on a private local model that needs no API key, or on an OpenAI-compatible cloud provider with your own key. How to choose a local model for your GPU."
icon: "brain"
---

The assistant runs on whatever model you choose. This is the single most important assistant setting — set it in *Settings → Assistant → Assistant brain*.

<CardGroup cols={2}>
  <Card title="On my device" icon="microchip">
    Download a private local model. It stays on this machine and needs no API key. First use also downloads the bundled local engine once, then runs fully offline.
  </Card>

  <Card title="Cloud provider" icon="cloud">
    Answer through an OpenAI-compatible provider — cloud, or your own Ollama or LM Studio server — using your own API key.
  </Card>
</CardGroup>

## Choosing a local model

The catalog offers three conversation-first choices: quickest, recommended, and more capable but slower. All three see images, so any of them can do [screen vision](/assistant/screen-vision).

| Model | Download | Choose it when |
| --- | --- | --- |
| **Gemma 4 E2B** | ~4 GB | You want the quickest replies and can accept weaker answers on complex requests. |
| **Gemma 4 E4B** (recommended) | ~5.9 GB | You want the best balance of quality and speed for conversation. |
| **Gemma 4 12B** | ~6.8 GB | You want sharper answers on nuanced questions and have a strong GPU to absorb the latency. |

Before you pick, the catalog reports the GPU it detected and how much graphics memory it has — dedicated VRAM, shared memory, or a plain "graphics memory unavailable" when it can't tell. That last case is honest rather than reassuring: with no GPU information, response speed genuinely can't be predicted, and you should expect CPU-speed replies.

<Note>
  These models are multimodal, so each one downloads a companion projector file automatically alongside the weights. The sizes above already include it.
</Note>

Need something else — a different size, a different family, a non-vision model? Search Hugging Face for any GGUF from the same screen. See [Custom models](/models/custom-models).

Next: [Providers & keys](/assistant/providers) for cloud and local-server setup, plus the local engine's context-window and memory-unload options.
